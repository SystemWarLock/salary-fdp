    - type: web
      name: salary-api
      runtime: python 
      buildCommand: pip install -r requirements.txt
      StartCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
 #YAML is for development pipeline codes
